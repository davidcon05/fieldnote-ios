# Test dashboard empty state
appId: com.davidcon.fieldnote
---
- launchApp:
    clearState: true

# Verify empty state appears
- assertVisible: "Journals"
- assertVisible: "No Journals Yet"
- assertVisible: "Start by adding a new journal"
- assertVisible: "New Journal"
